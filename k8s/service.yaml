apiVersion: v1
kind: Service
metadata:
  name: my-node-api-service
spec:
  type: NodePort
  selector:
    app: my-node-api
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080
